# services/api

**Milestone: M2.** Hono + tRPC server (deployed to Fly.io, Zürich). Exposes
`product.list` / `search` / `sortBy(metric)` reading the synced catalog from Supabase
Postgres via the Supabase pooler. Versioned at `/trpc/v1`; rate-limited.

_Placeholder — scaffolded in M2._
