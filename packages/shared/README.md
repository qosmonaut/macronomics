# packages/shared

**Milestone: M2.** Cross-cutting code shared by client and server: zod schemas, the exported
tRPC router type, the macro/ratio math (protein per CHF, protein/kcal, …), and shared
constants. Keeps a single source of truth for types across the monorepo.

_Placeholder — scaffolded in M2._
